public class Person {
  private enum Gender {
    MALE, FEMALE
  }
  private String name;
  private int age;
  private Gender gender;
}

public class Company {
  private String name;
}

public class Employee {
  private Person person;
  private Company company;
}

GroupedAndSortedEmployees[][] = new function(Employees[]) {
  this.getClass()[] arr = Employees.groupBy(this.getCompany());
  arr[][?] ->
  for(this.getClass()[] subArr: arr[][]
  Array.dimension(1, )
}

public class Array {
  public ?[]
  //returns the dimensi
}

Array[][] o---o @(ArrayDimension(1)) {

}

E[] -=> E[][] {
  e.groupBy(_**val)
  obj[]
}

Employees[] -=> GroupedAndSortedEmployees[][] {
  groups[] = {
    @INPUT@Dimension(1) {
      .groupBy(_**company, [method = "variable.value", returnSort = "alphabetized"])
    }
  }
  groups

}


@INPUT@Dimension(1) {.groupBy(_**company, [method = "variable.value", returnSort = "alphabetized"])

@Dim(1):
  group[][]= groupBy(this.company) -> new [] -> {.forEach(obj -)}
  group.forEach(sortBy(name, alphabetized))

GroupedAndSortedEmployees = Employees.stream().groupBy(it.getCompany()).forEach(sortBy(it.getName(), order("alphabetized"))).group().groupBy(each[].company);


Employee[] Employees;
Employee[][] GroupedAndSortedEmployees;
GroupedAndSortedEmployees = Employees.stream().groupBy(this.getCompany()).forEach(sortBy(this.getName(), order("alphabetized"))).collect().groupBy(this.company);
